"use client";

import { motion } from "framer-motion";
import { ArrowUpRight, Check, CircleDot, Compass, Globe2, ShieldCheck, Sparkles, Waypoints } from "lucide-react";
import Link from "next/link";
import { useLocale, useTranslations } from "next-intl";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { formatScore } from "@/lib/utils/format";

export function LandingPage() {
  const locale = useLocale();
  const t = useTranslations("landing");
  const c = useTranslations("common");
  const arrowClass = locale === "fa" ? "rotate-180" : "";
  return <div className="relative overflow-hidden"><div className="shell-grid pointer-events-none absolute inset-0 -z-10 h-[680px]" /><section className="mx-auto grid max-w-7xl items-center gap-14 px-5 pb-24 pt-20 sm:px-8 lg:grid-cols-[1.02fr_.98fr] lg:gap-20 lg:px-10 lg:pb-32 lg:pt-28"><motion.div initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .55 }}><Badge tone="accent"><Sparkles size={12} />{t("eyebrow")}</Badge><h1 className="display text-balance mt-7 max-w-3xl text-[clamp(3.2rem,7vw,6.6rem)] font-black text-[var(--ink)]">{t("title")}</h1><p className="mt-7 max-w-xl text-lg leading-8 text-[var(--muted)]">{t("subtitle")}</p><div className="mt-9 flex flex-wrap gap-3"><Button asChild size="lg"><Link href={`/${locale}/jobs`}>{t("explore")}<ArrowUpRight size={17} className={arrowClass} /></Link></Button><Button asChild variant="secondary" size="lg"><Link href={`/${locale}/onboarding`}>{t("build")}</Link></Button></div><div className="mt-10 flex flex-wrap items-center gap-5 text-xs font-semibold text-[var(--muted)]"><span className="flex items-center gap-2"><ShieldCheck size={15} className="text-[var(--mint)]" />{c("verified")}</span><span className="flex items-center gap-2"><CircleDot size={15} className="text-[var(--accent)]" />{t("deterministic")}</span><span className="flex items-center gap-2"><Globe2 size={15} className="text-[var(--amber)]" />{t("europeFocused")}</span></div></motion.div><motion.div initial={{ opacity: 0, scale: .97 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: .12, duration: .65 }} className="relative"><div className="absolute -inset-5 rounded-[3rem] bg-[var(--accent)]/10 blur-3xl" /><div className="relative overflow-hidden rounded-[2rem] border border-[var(--line)] bg-[var(--card)] p-3 card-shadow"><div className="rounded-[1.35rem] border border-[var(--line)] bg-[var(--paper)] p-5 sm:p-7"><div className="flex items-center justify-between"><div><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.14em] text-[var(--muted)]"><Compass size={13} className="text-[var(--accent)]" />{t("radar")}</div><p className="mt-2 text-xl font-black tracking-[-.04em] text-[var(--ink)]">{t("shortlist")}</p></div><div className="grid size-11 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><Waypoints size={20} /></div></div><div className="mt-7 space-y-3"><PreviewRow title={t("visaEvidence")} value={t("strongSignal")} tone="success" progress={92} locale={locale} /><PreviewRow title={t("skillAlignment")} value={t("profileFit")} tone="accent" progress={86} locale={locale} /><PreviewRow title={t("countryPreference")} value={t("goodFit")} tone="warning" progress={74} locale={locale} /></div><div className="mt-7 rounded-2xl border border-[var(--line)] bg-[var(--card)] p-4"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-[var(--muted)]">{t("recommendationLogic")}</p><p className="mt-1 text-sm font-bold text-[var(--ink)]">{t("everyScore")}</p></div><Check size={18} className="text-[var(--mint)]" /></div><div className="mt-4 flex gap-1.5">{["w-1/3", "w-1/4", "w-1/5", "w-1/6"].map((width, index) => <div key={width} className={`h-2 rounded-full ${index === 1 ? "bg-[var(--accent)]" : index === 2 ? "bg-[var(--mint)]" : "bg-[var(--line)]"} ${width}`} />)}</div></div></div></div><div className="absolute -bottom-7 -start-5 hidden rounded-2xl border border-[var(--line)] bg-[var(--card)] p-4 card-shadow sm:block"><p className="text-[10px] font-bold uppercase tracking-[.13em] text-[var(--muted)]">{t("signalQuality")}</p><p className="mt-1 text-xl font-black text-[var(--ink)]">{t("evidenceFirst")}</p></div></motion.div></section><section className="border-y border-[var(--line)] bg-[var(--card)]"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-16 sm:px-8 lg:grid-cols-[.7fr_1.3fr] lg:px-10 lg:py-24"><div><p className="text-sm font-bold uppercase tracking-[.16em] text-[var(--accent)]">{t("workflow")}</p><h2 className="display mt-4 max-w-sm text-4xl font-black text-[var(--ink)] sm:text-5xl">{t("workflowBody")}</h2></div><div className="grid gap-4 sm:grid-cols-3"><Step number={t("stepOne")} icon={<Compass size={18} />} body={t("radarBody")} /><Step number={t("stepTwo")} icon={<ShieldCheck size={18} />} body={t("signalBody")} /><Step number={t("stepThree")} icon={<ArrowUpRight size={18} className={arrowClass} />} body={t("trust")} /></div></div></section><section className="mx-auto max-w-7xl px-5 py-20 text-center sm:px-8 lg:px-10 lg:py-28"><p className="mx-auto max-w-2xl text-2xl font-black tracking-[-.04em] text-[var(--ink)] sm:text-4xl">{t("trust")}</p><Button asChild className="mt-8"><Link href={`/${locale}/onboarding`}>{t("build")}<ArrowUpRight size={16} className={arrowClass} /></Link></Button></section></div>;
}

function PreviewRow({ title, value, tone, progress, locale }: { title: string; value: string; tone: "success" | "accent" | "warning"; progress: number; locale: string }) {
  const color = tone === "success" ? "bg-[var(--mint)]" : tone === "warning" ? "bg-[var(--amber)]" : "bg-[var(--accent)]";
  return <div className="grid grid-cols-[1fr_auto] items-center gap-4 rounded-2xl border border-[var(--line)] bg-[var(--card)] p-4"><div><div className="flex items-center justify-between gap-3"><p className="text-sm font-bold text-[var(--ink)]">{title}</p><span className="text-xs font-bold text-[var(--muted)]">{formatScore(progress, locale)}</span></div><div className="mt-3 h-1.5 rounded-full bg-[var(--line)]"><div className={`h-full rounded-full ${color}`} style={{ width: `${progress}%` }} /></div></div><span className="text-[11px] font-bold text-[var(--muted)]">{value}</span></div>;
}

function Step({ number, icon, body }: { number: string; icon: React.ReactNode; body: string }) {
  return <motion.div whileHover={{ y: -4 }} className="rounded-3xl border border-[var(--line)] bg-[var(--paper)] p-5"><div className="flex size-10 items-center justify-center rounded-xl bg-[var(--accent-soft)] text-[var(--accent)]">{icon}</div><p className="mt-6 text-[11px] font-bold uppercase tracking-[.12em] text-[var(--accent)]">{number}</p><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{body}</p></motion.div>;
}
