"use client";

import { zodResolver } from "@hookform/resolvers/zod";
import { ArrowLeft, ArrowRight, Check, LoaderCircle, MapPin, Plus, Sparkles } from "lucide-react";
import Link from "next/link";
import { useLocale, useTranslations } from "next-intl";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { useForm } from "react-hook-form";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
import { useCreateCandidate } from "@/lib/api/hooks";
import { candidateSchema, type CandidateFormValues } from "@/lib/validators/candidate";
import { cn } from "@/lib/utils/cn";

const roles = ["AI / Machine Learning", "Backend Engineering", "Frontend Engineering", "Data Science", "Data Engineering", "DevOps / Cloud"];
const skills = ["Python", "TypeScript", "SQL", "PyTorch", "Machine Learning", "Docker", "Kubernetes", "AWS", "React", "FastAPI"];
const countries = ["Germany", "Netherlands", "Sweden", "Denmark", "Finland", "Ireland", "United Kingdom"];
const steps = ["role", "skills", "experience", "countries", "visa", "remote"] as const;

export function OnboardingPage() {
  const locale = useLocale();
  const t = useTranslations("onboarding");
  const router = useRouter();
  const [step, setStep] = useState(0);
  const mutation = useCreateCandidate();
  const { register, handleSubmit, watch, setValue, trigger, formState: { errors } } = useForm<CandidateFormValues>({ resolver: zodResolver(candidateSchema), defaultValues: { name: "", target_roles: [], skills: [], years_of_experience: 3, seniority: "mid", preferred_countries: [], visa_required: true, relocation_preference: "preferred", remote_preference: "no_preference", excluded_locations: [] } });
  const selectedRoles = watch("target_roles");
  const selectedSkills = watch("skills");
  const selectedCountries = watch("preferred_countries");
  const toggle = (field: "target_roles" | "skills" | "preferred_countries", value: string) => { const current = watch(field); setValue(field, current.includes(value) ? current.filter((item) => item !== value) : [...current, value], { shouldValidate: true }); };
  const submit = async (values: CandidateFormValues) => { const candidate = await mutation.mutateAsync(values); window.localStorage.setItem("career-radar-candidate", String(candidate.id)); router.push(`/${locale}/dashboard`); };
  const next = async () => { const fields = step === 0 ? ["name", "target_roles"] : step === 1 ? ["skills"] : step === 3 ? ["preferred_countries"] : []; const valid = fields.length === 0 || await trigger(fields as (keyof CandidateFormValues)[]); if (valid) setStep((current) => Math.min(current + 1, steps.length - 1)); };
  return <div className="min-h-[calc(100vh-72px)] px-5 py-10 sm:px-8 lg:px-10 lg:py-16"><div className="mx-auto max-w-4xl"><Link href={`/${locale}`} className="focus-ring inline-flex items-center gap-2 text-sm font-bold text-[var(--muted)] hover:text-[var(--accent)]"><ArrowLeft size={15} />{t("back")}</Link><div className="mt-8 grid gap-8 lg:grid-cols-[.7fr_1.3fr]"><aside className="rounded-3xl bg-[var(--ink)] p-7 text-[var(--paper)] sm:p-9"><div className="grid size-11 place-items-center rounded-2xl bg-white/10 text-[var(--accent)]"><Sparkles size={20} /></div><p className="mt-8 text-xs font-bold uppercase tracking-[.15em] text-white/50">Career Radar / Profile</p><h1 className="mt-4 text-3xl font-black leading-tight tracking-[-.05em]">{t("title")}</h1><p className="mt-4 text-sm leading-7 text-white/60">{t("subtitle")}</p><div className="mt-10 space-y-3">{steps.map((item, index) => <div key={item} className={cn("flex items-center gap-3 text-sm", index === step ? "font-bold text-white" : index < step ? "text-[var(--mint)]" : "text-white/35")}><span className={cn("grid size-7 place-items-center rounded-full border text-xs", index < step ? "border-[var(--mint)] bg-[var(--mint)] text-[var(--ink)]" : index === step ? "border-[var(--accent)] bg-[var(--accent)] text-white" : "border-white/20")}>{index < step ? <Check size={13} /> : index + 1}</span>{t(item)}</div>)}</div></aside><Card className="p-6 sm:p-9"><form onSubmit={handleSubmit(submit)}><div className="min-h-[390px]">{step === 0 && <StepBlock title={t("role")}><label className="mb-6 block"><span className="mb-2 block text-xs font-bold text-[var(--muted)]">{t("name")}</span><Input {...register("name")} placeholder={t("placeholder")} autoFocus />{errors.name && <ErrorText text={errors.name.message} />}</label><div className="grid gap-3 sm:grid-cols-2">{roles.map((role) => <Choice key={role} selected={selectedRoles.includes(role)} onClick={() => toggle("target_roles", role)}>{role}</Choice>)}</div>{errors.target_roles && <ErrorText text={errors.target_roles.message} />}</StepBlock>}{step === 1 && <StepBlock title={t("skills")}><p className="mb-5 text-sm text-[var(--muted)]">Choose the capabilities that should influence your shortlist.</p><div className="flex flex-wrap gap-2">{skills.map((skill) => <Choice key={skill} compact selected={selectedSkills.includes(skill)} onClick={() => toggle("skills", skill)}>{skill}</Choice>)}</div></StepBlock>}{step === 2 && <StepBlock title={t("experience")}><div className="grid gap-3 sm:grid-cols-2"><Choice selected={watch("seniority") === "junior"} onClick={() => setValue("seniority", "junior")}>Early career</Choice><Choice selected={watch("seniority") === "mid"} onClick={() => setValue("seniority", "mid")}>Mid-level</Choice><Choice selected={watch("seniority") === "senior"} onClick={() => setValue("seniority", "senior")}>Senior</Choice><Choice selected={watch("seniority") === "staff"} onClick={() => setValue("seniority", "staff")}>Staff / Lead</Choice></div><label className="mt-8 block"><span className="mb-2 block text-xs font-bold text-[var(--muted)]">Years of experience</span><Input type="number" min="0" max="60" step="0.5" {...register("years_of_experience", { valueAsNumber: true })} /></label></StepBlock>}{step === 3 && <StepBlock title={t("countries")}><p className="mb-5 text-sm text-[var(--muted)]">Select every country you would seriously consider.</p><div className="grid gap-3 sm:grid-cols-2">{countries.map((country) => <Choice key={country} selected={selectedCountries.includes(country)} onClick={() => toggle("preferred_countries", country)}><MapPin size={15} />{country}</Choice>)}</div></StepBlock>}{step === 4 && <StepBlock title={t("visa")}><div className="space-y-3"><Choice selected={watch("visa_required") === true} onClick={() => setValue("visa_required", true)}>Yes, I need employer support</Choice><Choice selected={watch("visa_required") === false} onClick={() => setValue("visa_required", false)}>No, I already have work authorization</Choice></div></StepBlock>}{step === 5 && <StepBlock title={t("remote")}><div className="space-y-3"><Choice selected={watch("remote_preference") === "preferred"} onClick={() => setValue("remote_preference", "preferred")}>Remote is important</Choice><Choice selected={watch("remote_preference") === "no_preference"} onClick={() => setValue("remote_preference", "no_preference")}>I’m open to the right setup</Choice><Choice selected={watch("remote_preference") === "required"} onClick={() => setValue("remote_preference", "required")}>Remote is required</Choice></div></StepBlock>}</div>{mutation.isError && <p className="mt-4 rounded-xl bg-[var(--rose-soft)] px-3 py-2 text-sm text-[var(--rose)]">{mutation.error.message}</p>}<div className="mt-8 flex justify-between border-t border-[var(--line)] pt-5"><Button type="button" variant="ghost" disabled={step === 0} onClick={() => setStep((current) => current - 1)}><ArrowLeft size={15} />{t("back")}</Button>{step === steps.length - 1 ? <Button type="submit" disabled={mutation.isPending}>{mutation.isPending ? <LoaderCircle size={15} className="animate-spin" /> : <Plus size={15} />}{t("finish")}</Button> : <Button type="button" onClick={() => void next()}>{t("next")}<ArrowRight size={15} /></Button>}</div></form></Card></div></div></div>;
}

function StepBlock({ title, children }: { title: string; children: React.ReactNode }) { return <div><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--accent)]">Step in your radar</p><h2 className="mt-3 text-3xl font-black tracking-[-.05em] text-[var(--ink)]">{title}</h2><div className="mt-8">{children}</div></div>; }
function Choice({ children, selected, onClick, compact = false }: { children: React.ReactNode; selected: boolean; onClick: () => void; compact?: boolean }) { return <button type="button" onClick={onClick} className={cn("focus-ring flex items-center gap-3 rounded-2xl border text-start text-sm font-bold transition", compact ? "px-3 py-2.5" : "px-4 py-4", selected ? "border-[var(--accent)] bg-[var(--accent-soft)] text-[var(--accent-strong)]" : "border-[var(--line)] bg-[var(--card)] text-[var(--muted)] hover:border-[var(--accent)]/60 hover:text-[var(--ink)]")}>{selected ? <span className="grid size-5 shrink-0 place-items-center rounded-full bg-[var(--accent)] text-white"><Check size={12} /></span> : <span className="size-5 shrink-0 rounded-full border border-[var(--line)]" />}{children}</button>; }
function ErrorText({ text }: { text?: string }) { return text ? <p className="mt-2 text-xs font-semibold text-[var(--rose)]">{text}</p> : null; }
