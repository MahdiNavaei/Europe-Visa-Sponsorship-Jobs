import { ArrowUpRight, Building2, CheckCircle2, MapPin, ShieldCheck, Sparkles, TriangleAlert } from "lucide-react";
import Link from "next/link";
import { useLocale, useTranslations } from "next-intl";
import type { Job, Recommendation } from "@/lib/types";
import { formatDate, formatScore, labelize } from "@/lib/utils/format";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { ScoreBadge, ScoreBar } from "@/components/ui/progress";

export function JobCard({ job, recommendation }: { job: Job; recommendation?: Recommendation }) {
  const locale = useLocale();
  const t = useTranslations("common");
  const score = recommendation?.scores.overall ?? job.eligibility_score;
  const visa = recommendation?.scores.visa ?? job.eligibility_score;
  return <Card className="group overflow-hidden transition duration-300 hover:-translate-y-1 hover:border-[var(--accent)] hover:shadow-[var(--shadow-card)]"><div className="p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div className="flex min-w-0 gap-3"><div className="grid size-11 shrink-0 place-items-center rounded-2xl bg-[var(--accent-soft)] text-[var(--accent)]"><Building2 size={19} /></div><div className="min-w-0"><h3 className="truncate text-base font-black tracking-[-.025em] text-[var(--ink)] sm:text-lg">{job.title}</h3><p className="mt-1 truncate text-sm font-medium text-[var(--muted)]">{job.company_name}</p></div></div><ScoreBadge value={score} label={recommendation ? "Your match" : "Visa score"} tone={recommendation ? "accent" : "success"} /></div><div className="mt-5 flex flex-wrap gap-2"><Badge tone={job.eligibility_status === "eligible" ? "success" : job.eligibility_status === "unknown" ? "warning" : "danger"}><ShieldCheck size={12} />{job.eligibility_status === "eligible" ? t("eligible") : labelize(job.eligibility_status)}</Badge><Badge><MapPin size={12} />{(job.country ?? job.location) || "Europe"}</Badge><Badge tone="neutral">{labelize(job.job_family)}</Badge></div>{recommendation ? <div className="mt-5 grid gap-3 rounded-2xl bg-[var(--paper)] p-4 sm:grid-cols-2"><div><div className="flex items-center justify-between text-xs"><span className="font-semibold text-[var(--muted)]">Visa compatibility</span><span className="font-bold text-[var(--ink)]">{formatScore(visa)}</span></div><div className="mt-2"><ScoreBar value={visa} color="success" /></div></div><div><div className="flex items-center justify-between text-xs"><span className="font-semibold text-[var(--muted)]">Skill alignment</span><span className="font-bold text-[var(--ink)]">{formatScore(recommendation.scores.skill)}</span></div><div className="mt-2"><ScoreBar value={recommendation.scores.skill} /></div></div></div> : <p className="mt-5 line-clamp-2 text-sm leading-6 text-[var(--muted)]">{job.description || "Evidence and role details are available on the job page."}</p>}<div className="mt-5 flex items-center justify-between gap-3 border-t border-[var(--line)] pt-4"><div className="flex min-w-0 flex-wrap gap-x-4 gap-y-1 text-xs text-[var(--muted)]"><span className="flex items-center gap-1.5"><Sparkles size={13} className="text-[var(--accent)]" />{formatDate(job.posted_at, locale)}</span>{job.workplace_type && <span>{job.workplace_type}</span>}</div><Button asChild variant="ghost" size="sm"><Link href={`/${locale}/jobs/${job.id}`}>{t("viewDetails")}<ArrowUpRight size={15} /></Link></Button></div>{recommendation && (recommendation.reasons.length > 0 || recommendation.warnings.length > 0) && <div className="mt-4 space-y-2">{recommendation.reasons.slice(0, 2).map((reason) => <p key={reason} className="flex gap-2 text-xs leading-5 text-[var(--muted)]"><CheckCircle2 size={14} className="mt-0.5 shrink-0 text-[var(--mint)]" />{reason}</p>)}{recommendation.warnings.slice(0, 1).map((warning) => <p key={warning} className="flex gap-2 text-xs leading-5 text-[var(--muted)]"><TriangleAlert size={14} className="mt-0.5 shrink-0 text-[var(--amber)]" />{warning}</p>)}</div>}</div></Card>;
}
